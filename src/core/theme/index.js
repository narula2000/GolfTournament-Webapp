import { extendTheme } from '@chakra-ui/react';

const overrides = {
  colors: {
    white: 'white',
    primary: '#80D2F1',
    secondary: '#7FD661',
    error: '#f13a59',
    background: '#cfecc5',
  },
};

export default extendTheme(overrides);
